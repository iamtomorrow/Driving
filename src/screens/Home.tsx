import { View, Text } from "react-native"
import { Header } from "../components/Header/Header"

export const Home = ( ) => {
    return (
        <View
        style={{
            width: "100%",
            height: "100%",
            backgroundColor: "#ffffff"
        }}>
            <Header />

        </View>
    )
}